# Ch006 · H1 — 터미널·셸·Bash: 오리엔테이션 — 검은 화면이 다섯 손가락이 되는 길

> **이 H에서 얻을 것**
> - 터미널·셸·Bash 셋의 정의 차이 — 비슷한 말 같지만 셋이 다른 층
> - 4핵심 단어 — 터미널·셸·프로세스·파일시스템. 8H 큰그림의 토대
> - 한 명령어 0.30초 흐름 — 키보드 → 터미널 → 셸 → 커널 → 실행 → 출력 7단계
> - 8H 큰그림 — H2 핵심개념·H3 셋업·H4 명령어 카탈로그·H5 데모 한 줄·H6 운영 스크립트·H7 내부 동작·H8 적용
> - 자경단 5명에 본 챕터가 어떻게 박히는가 — 까미·노랭이·미니·깜장이·본인 각자의 alias

---

## 회수: Ch005의 도구에서 본 챕터의 손가락으로

지난 Ch005에서 본인은 5명 자경단의 협업 도구 30개를 봤어요. branch protection·CODEOWNERS·husky·30개 git/gh 명령어. 그건 **도구의 그림**.

이번 Ch006는 그 도구를 **본인이 직접 만드는 alias·script**로. 손가락이 도구가 되고, 도구가 손가락이 되는 양방향. 자경단의 30개 명령어를 본인이 매번 치는 건 비효율 — 자주 쓰는 13개를 alias로, 5단계 의식을 script로 박으면 손가락이 5분 작업을 5초로.

지난 Ch004 H1은 사진앨범 비유로 git을 소개했고, Ch005 H1은 5명 합의로 협업을 소개했어요. 이번 H1은 **검은 화면**의 비밀이에요. 터미널이 어떻게 본인의 키보드 입력을 0.3초에 컴퓨터의 작업으로 바꾸는가.

---

## 1. 검은 화면 — 왜 이걸 배우나, 7이유

본인의 자경단 노트북 화면 어딘가엔 검은 배경 + 흰 글씨 + 깜박이는 커서. 그 화면이 본인의 5년 개발 도구. **왜 GUI(아이콘 클릭)보다 터미널을 배우나** — 7이유.

### 1-1. 7이유

1. **자동화 가능** — 한 줄로 100파일 처리. GUI는 100번 클릭. 본인의 자경단 강의 챕터 120개 처리 같은 작업이 한 줄.
2. **원격 서버 표준** — AWS·GCP·prod 서버는 GUI 없음. SSH로 터미널만. **서버 만질 수 있어야 인프라**.
3. **복사·자동화** — 한 명령어를 5명이 같이 사용. GUI는 5번 시연. **복사가 협업의 곱셈**.
4. **속도** — 손가락이 마우스보다 5배 빠름. 5년 동안 5만 번 작업하면 차이 무한.
5. **AI 시대의 손가락** — Claude·ChatGPT가 본인에게 명령어 추천. 받아서 한 번에 실행. **AI 친구가 터미널만 보내요**.
6. **gits·docker·k8s 모두 CLI** — 모든 개발 도구가 터미널 우선. GUI는 그 위의 wrapper.
7. **면접 단골** — "터미널에서 가장 큰 파일 찾는 법?" 면접 한 줄 질문. 답이 1초면 시니어, 1분이면 신입.

본인이 검은 화면을 5년 후엔 IDE·브라우저보다 더 자주 봐요. **검은 화면이 본인의 평생 도구**.

### 1-2. 한 줄 데모 — "내 맥에서 가장 큰 파일 5개"

```bash
$ find ~ -type f -size +100M -exec ls -lh {} \; 2>/dev/null | sort -k5 -hr | head -5
-rw-r--r--  1 mo  staff  4.2G  Apr 28 ~/Downloads/macos-installer.dmg
-rw-r--r--  1 mo  staff  2.1G  Mar 12 ~/Library/.../iPhoneSimulator.dmg
...
```

한 줄로 5초. GUI라면 Finder 5번 + 정렬 + 5분. **한 줄이 5분의 차이**. 본 H가 끝날 때 본인은 이 한 줄을 읽을 수 있어요.

---

## 2. 터미널·셸·Bash 셋의 정의

비슷한 말 같지만 셋이 다른 층.

### 2-1. 터미널 (Terminal)

**검은 창** 자체. 키보드 입력을 받아 글자를 화면에 그리는 GUI 앱. macOS의 `Terminal.app`·`iTerm2`·`Warp`, Windows의 `Windows Terminal`·`ConEmu`, Linux의 `gnome-terminal`.

옛날엔 진짜 하드웨어 — 1970년대 IBM의 검은 박스 + 키보드 + CRT 화면. 컴퓨터 본체와 케이블로 연결. 그 박스가 "terminal" (끝점). 1980년대부터 소프트웨어로 시뮬레이션 (`xterm`). 지금 본인이 macOS Terminal.app 켜면 옛날 하드웨어를 흉내내는 소프트웨어.

### 2-2. 셸 (Shell)

터미널 안에서 실행되는 **프로그램**. 본인 입력을 받아 명령어로 해석하고 실행. 옛날 — `sh` (1971·Bourne shell). 지금 — `bash`·`zsh`·`fish`·`pwsh`.

**자경단 표준 — zsh** (macOS Catalina 2019부터 기본). bash는 Linux 표준. 둘 거의 호환.

### 2-3. Bash (Bourne-Again SHell)

특정 셸의 한 종류. 1989년 Brian Fox 작성. GNU 프로젝트의 표준. Linux의 기본. macOS도 Catalina 전엔 기본.

**자경단 표준 — Bash 호환**. zsh도 거의 호환이라 같은 스크립트 작동.

### 2-4. 한 표

| 층 | 무엇 | 예 | 자경단 표준 |
|----|------|-----|-----------|
| 1. 터미널 (앱) | 키보드·글자 그림 | Terminal.app·iTerm2·Warp | iTerm2 |
| 2. 셸 (프로그램) | 명령어 해석 | bash·zsh·fish·pwsh | zsh (macOS 기본) |
| 3. Bash (셸 종류) | GNU 표준 | bash 5.x | bash 호환 |

**관계** — 터미널이 컨테이너, 셸이 그 안의 프로그램, Bash는 셸 한 종류.

비유 — 터미널 = 텍스트 에디터 창, 셸 = 사용 중인 언어(영어·한국어), Bash = 영어의 한 방언(미국식·영국식).

---

## 3. 4핵심 단어 — 터미널·셸·프로세스·파일시스템

본 챕터 8H의 토대 4개.

### 3-1. 터미널

위에서 봤듯 검은 창. **TTY** (TeleTYpe) 라는 옛날 이름이 그대로. `tty` 명령어로 본인의 터미널 ID 확인:

```bash
$ tty
/dev/ttys003
```

### 3-2. 셸

명령어 해석. 본인이 어느 셸 쓰고 있는지:

```bash
$ echo $0
zsh                    # 현재 사용 셸
$ echo $SHELL
/bin/zsh               # 기본 셸 (login shell)
```

### 3-3. 프로세스

실행 중인 프로그램. 본인이 한 명령어를 치면 셸이 새 프로세스를 fork·exec. `ps` 명령어로 본인 프로세스 목록:

```bash
$ ps
  PID TTY           TIME CMD
12345 ttys003   0:00.05 -zsh
67890 ttys003   0:00.01 ps
```

PID 12345 = 본인의 zsh 셸. 67890 = `ps` 명령 자체.

### 3-4. 파일시스템

`/` (루트)부터 시작하는 디렉토리·파일 트리. 모든 명령어가 파일에서 시작·파일에 쓰기. **모든 것이 파일**이라는 Unix 철학.

```bash
$ pwd
/Users/mo
$ ls /
Applications  Library  System  Users  bin  etc  tmp  ...
```

### 3-5. 4단어의 관계

```
[터미널 창]
   └── [셸 프로세스 (zsh)]
          └── [실행한 명령어 프로세스 (ls·grep·등)]
                 └── [파일시스템 접근]
```

본인이 `ls /`를 친 한 줄:
1. **터미널**이 키보드 입력 받음
2. **셸**(zsh)이 "ls /"를 해석
3. **셸**이 자식 **프로세스** fork
4. 자식이 `/usr/bin/ls`를 exec
5. ls가 **파일시스템** `/`를 read
6. 결과 출력 → 터미널이 그림

**4 단어가 한 줄 명령어의 흐름**.

---

## 4. 한 명령어 0.30초 — 7단계 흐름

본인이 `ls -la` 친 그 0.30초 안에 7단계.

```
0.000초  [본인 키보드] l-s-space-Enter
0.005초  [터미널] 글자를 셸에 stdin으로 전달
0.010초  [셸] "ls" 파싱 + 옵션 -la 인식
0.015초  [셸] $PATH 검색 → /usr/bin/ls 찾음
0.020초  [셸] fork() → 자식 프로세스 생성
0.025초  [자식] exec("/usr/bin/ls", ["-la"]) → ls 코드 로드
0.100초  [ls] 현재 디렉토리 read + 파일 5~50개 stat
0.250초  [ls] stdout으로 출력 → 터미널이 그림
0.300초  [셸] 자식 종료 wait + 프롬프트 다시
```

**0.30초 안에 7단계**. 본인이 1초 동안 3번 명령어 가능. 5명 자경단이 5분 동안 1,000번 명령어 가능. **속도가 자경단의 비밀**.

---

## 5. 자경단 5명 적용 — 각자의 alias 풍경

자경단 5명이 본 챕터를 어떻게 적용하는가.

### 5-1. 5명 alias 풍경

| 누구 | 자주 쓰는 명령어 | 자경단 alias |
|------|---------------|------------|
| **본인** (메인테이너) | git status·log·gh pr list | `s`·`lg`·`mypr` |
| **까미** (백엔드) | curl·jq·grep·docker | `cj`·`g`·`d` |
| **노랭이** (프론트) | npm run·node·prettier | `nr`·`np`·`pf` |
| **미니** (인프라) | ssh·scp·terraform·aws | `vps`·`tf`·`aw` |
| **깜장이** (디자인·QA) | playwright·screenshot | `pw`·`ss` |

5명이 각자 5~10개 alias. 매일 100번 사용. 1년 36,500번. alias 한 줄이 매일 30초 절약 = 5명 × 1년 = 600시간.

### 5-2. 자경단 공통 alias 5종

`~/.zshrc` 또는 `~/.bashrc`에 5명 모두 박는 공통 5종:

```bash
# 5명 공통 (.zshrc)
alias s='git status -sb'
alias lg='git log --oneline --graph --all -20'
alias ll='ls -lah'
alias cd..='cd ..'
alias mypr='gh pr list --search "review-requested:@me"'
```

5종 × 5명 = 25개 일일 손가락 절약. 5년이면 125시간. **5줄이 평생**.

### 5-3. 자경단 시뮬레이션 — 첫 alias 설정 5분

본인이 자경단 합류 첫 날 5분:

```bash
# 1. ~/.zshrc 열기
$ open ~/.zshrc                  # macOS — VS Code 또는 Finder
# 또는
$ vim ~/.zshrc                   # 터미널 에디터

# 2. 위 5종 alias 추가
# 3. 저장 + 셸 재시작
$ source ~/.zshrc                # 또는 새 터미널 창

# 4. 검증
$ s                              # git status -sb 발동
$ ll                             # ls -lah 발동
```

5분 셋업이 매일 1분 절약 = 1년 6시간 + 5년 30시간.

---

## 6. 8H 큰그림

| H | 슬롯 | 무엇을 다루나 |
|---|------|------------|
| H1 | 오리엔 | 본 H — 검은 화면의 7이유, 4핵심 단어, 0.30초 흐름, 자경단 alias |
| H2 | 핵심개념 | 셸 변수·환경변수·PATH·exit code·subshell·glob·redirection·heredoc 등 8개념 깊이 |
| H3 | 환경점검 | iTerm2·zsh·oh-my-zsh·starship·brew 셋업 + dotfiles 5분 |
| H4 | 명령카탈로그 | 일상 명령어 30개 + 위험도 신호등 — ls·cd·mkdir·cp·mv·rm·find·grep·sed·awk·sort·uniq·wc·head·tail·xargs 등 |
| H5 | 데모 | "내 맥에서 가장 큰 파일 5개" 한 줄 + 자경단 일일 30개 명령어 시연 |
| H6 | 운영/스크립트 | bash 스크립트 작성 — `set -euo pipefail`·function·트랩·옵션 파싱·로그 |
| H7 | 원리/내부 | fork-exec·process group·세션·signal·redirection 내부·환경변수 inheritance |
| H8 | 적용+회고 | Ch006 마무리·자경단 dotfiles·Ch007(Python 입문) 예고 |

---

## 7. 12회수 지도 — 본 챕터가 다른 챕터에서 다시 만나는 곳

| 챕터 | 만나는 주제 |
|------|----------|
| Ch007 Python 입문 1 | `python script.py` 셸에서 실행 |
| Ch008 Python 입문 2 | 셸 if·for vs Python if·for |
| Ch013 모듈·패키지 | `python -m`·sys.argv·환경변수 |
| Ch014 venv·pip | `python -m venv`·activate 스크립트 |
| Ch020 typing | mypy CLI 사용 |
| Ch022 pytest | pytest CLI·conftest·markers |
| Ch041 백엔드 시작 | uvicorn·gunicorn 서버 실행 |
| Ch062 풀스택 통합 | docker-compose CLI |
| Ch091 AWS 배포 | aws CLI·SSH·rsync |
| Ch103 CI/CD | GitHub Actions의 bash steps |
| Ch118 면접 | "터미널 한 줄로 X 하기" 단골 |
| Ch120 회고 | 5년 dotfiles의 진화 |

본 챕터를 깊이 보면 12 챕터의 셸 부분이 매끈히.

---

## 8. 흔한 오해 5가지

**오해 1: "터미널 = 셸 = Bash."** — 셋이 다른 층. 터미널(앱)·셸(프로그램)·Bash(셸 한 종류). H2에서 깊이.

**오해 2: "GUI 시대에 터미널은 옛날 도구."** — AI 시대일수록 터미널 더 중요. Claude·ChatGPT가 본인에게 명령어 추천. AI 친구가 터미널을 통해 일함.

**오해 3: "맥북 사용자는 터미널 안 배워도 돼요."** — 자경단의 5명은 모두 터미널. 맥북 + iTerm2가 자경단 표준. macOS도 Unix 기반.

**오해 4: "셸 명령어는 외우기 어려워."** — 30개 핵심만 매일 반복. 1주일이면 손가락 자동. 5년 후엔 100개도 쉽게.

**오해 5: "터미널 사고는 무서워."** — 위험도 신호등(H4)으로 분류. 빨강 5개만 1초 호흡, 나머지 25개는 안전. 사고 가능성 1%.

---

## 9. FAQ 5가지

**Q1. zsh vs bash 어느 걸 써야?**
A. 자경단 표준 zsh (macOS 기본 + oh-my-zsh 풍부). bash는 Linux 서버 표준이라 둘 다 알면 좋음. 90% 호환.

**Q2. PowerShell도 비슷한가요?**
A. PowerShell은 Windows 표준 + 객체 기반 (Bash는 텍스트 기반). 차이 큼. 자경단 macOS 표준은 zsh.

**Q3. 터미널 멀티 창은 어떻게?**
A. iTerm2의 Cmd+T (탭) + Cmd+D (수직 분할) + Cmd+Shift+D (수평 분할). 한 화면에 4개 창 가능. tmux도 권장 (H3에서).

**Q4. 본인이 zsh 함정을 만나면?**
A. zsh의 글로브·alias 함정은 H2·H4에서 깊이. `setopt nomatch` 같은 옵션으로 우회.

**Q5. 본 챕터의 분량 8H가 너무 많지 않나요?**
A. 셸은 5년 도구라 깊이 있게. 처음 1주일 H1·H2 + 1주일 H3·H4 + 1주일 H5·H6 + 1주일 H7·H8 = 4주. 8주 슬랙 갖고 충분.

---

## 10. 셸의 진화 50년 — Thompson sh부터 AI 시대 Warp까지

본인이 지금 쓰는 zsh의 조상.

| 연도 | 셸 | 누가 | 무엇이 새로 |
|------|-----|------|----------|
| 1971 | **Thompson sh** | Ken Thompson (Bell Labs) | 첫 Unix 셸. 단순한 명령 실행 |
| 1977 | **Bourne sh** | Stephen Bourne (Bell Labs) | sh의 새 버전. 변수·제어 흐름 추가 |
| 1978 | **C shell** (csh) | Bill Joy (Berkeley) | C 문법. history·alias 도입 |
| 1989 | **Bash** | Brian Fox (FSF) | Bourne-Again. GNU 표준. csh + Bourne 통합 |
| 1990 | **zsh** | Paul Falstad (Princeton) | bash + 인터랙티브 강화. tab 자동완성·테마 |
| 2005 | **fish** | Axel Liljencrantz | 사용자 친화 문법. 색깔·문법 검사 |
| 2019 | **macOS Catalina** | Apple | 기본 셸 bash → zsh 변경. 라이선스 이유(GPL) |
| 2022 | **Warp** | Warp Inc | AI 통합 터미널. 명령어 추천·블록 단위 |
| 2024 | **Cursor·Claude Code** | Anthropic 등 | AI가 직접 명령어 생성·실행 |

**50년 진화의 한 줄** — sh(1971) → bash(1989) → zsh(1990) → AI 시대 Warp(2022). 본인이 쓰는 zsh가 50년 진화의 정점이에요. **50년이 본인의 매일 1만 번 손가락**.

자경단 표준 — 2025년 시점 zsh + iTerm2. 1년 후 Warp 또는 Claude Code 도입 검토. **시대가 도구를 만들고, 도구가 협업을 만들어요**.

---

## 11. 자경단 5년 dotfiles — 한 페이지 .zshrc 50줄

본인의 자경단 5년 후 `.zshrc`가 어떻게 진화하나. 50줄 한 페이지.

```bash
# ~/.zshrc — 자경단 본인 (메인테이너)
# 5년 누적 200줄 중 핵심 50줄

# 1. PATH (5줄)
export PATH="$HOME/.local/bin:$PATH"
export PATH="/opt/homebrew/bin:$PATH"           # Apple Silicon brew
export PATH="$HOME/.cargo/bin:$PATH"            # Rust
export PATH="$HOME/.bun/bin:$PATH"              # Bun
export PATH="$HOME/go/bin:$PATH"                # Go

# 2. 환경변수 (5줄)
export EDITOR="code --wait"
export LANG="en_US.UTF-8"
export HOMEBREW_NO_ANALYTICS=1
export NODE_OPTIONS="--max-old-space-size=4096"
export GH_TOKEN="$(cat ~/.config/gh/token)"

# 3. 자경단 alias 5종 (5줄)
alias s='git status -sb'
alias lg='git log --oneline --graph --all -20'
alias ll='ls -lah'
alias mypr='gh pr list --search "review-requested:@me"'
alias fpush='git push --force-with-lease'

# 4. 자경단 function (10줄)
gco() { git switch "$1"; }                     # gco main
gcb() { git switch -c "$1"; }                  # gcb feat/cat-card
catlog() { git log --grep="$1" --oneline; }    # catlog cat-card
prurl() { gh pr view --web; }
ll-size() { du -sh * | sort -hr | head -10; }   # 큰 파일 순

# 5. 셸 옵션 (5줄)
setopt nomatch                                  # 글로브 매치 없으면 에러
setopt no_share_history                         # 셸 간 history 분리
setopt prompt_subst
HISTSIZE=10000
SAVEHIST=10000

# 6. plugin (oh-my-zsh) (10줄)
ZSH=$HOME/.oh-my-zsh
plugins=(git docker kubectl npm rust gh)
ZSH_THEME="robbyrussell"
source $ZSH/oh-my-zsh.sh

# 7. starship 프롬프트 (5줄)
eval "$(starship init zsh)"

# 8. 자경단 함수 (5줄)
quickfix() {
  git switch main && git pull --rebase && git switch -c "fix/$1"
}
```

**50줄이 5년의 손가락**. 본인이 첫 1주일에 5줄 시작 → 1개월 20줄 → 6개월 50줄 → 5년 200줄. **줄 한 줄이 매일 30초**.

자경단 5명의 dotfiles를 GitHub repo (`cat-vigilante/dotfiles`)에 공유. 5명의 한 페이지가 1,000줄. **dotfiles가 협업의 보너스**.

---

## 12. AI 시대의 셸 — Claude·Cursor·Warp의 새 패러다임

2024년부터 셸이 AI와 통합. 본인의 자경단 5년 후엔 셸이 어떻게 바뀌나.

### 12-1. 옛 패러다임 (2023년까지)

본인이 명령어를 모르면 → Google 검색 → Stack Overflow → 복사 → 셸에 붙여 넣기 → 한 번 더 물어 → 결국 한 줄. **5분의 검색 + 1분 적용**.

### 12-2. 새 패러다임 (2024년 이후)

본인이 명령어를 모르면 → Claude·ChatGPT에 한 줄 질문 → AI가 한 줄 답 → 본인이 셸에 한 번 실행 → 1초. **5분 → 1초**.

### 12-3. AI가 셸을 직접 — Claude Code·Warp

더 깊은 진화 — AI가 본인 노트북의 셸에 직접 명령어를 실행. 본인은 결과만 보고 OK·NO. Claude Code의 Bash 도구·Warp의 AI 명령. **본인의 손가락이 AI에 위임**.

### 12-4. 자경단의 AI 시대 손가락

자경단 5명이 2026년에 사용하는 AI 셸 도구:
- 본인 — Claude Code (전체 자경단 강의 작성·코드 리뷰)
- 까미 — Cursor (백엔드 자동완성)
- 노랭이 — Warp (프론트 작업 + AI 명령)
- 미니 — gh-copilot (GitHub Actions 디버깅)
- 깜장이 — ChatGPT (디자인 ↔ 코드 변환)

5명이 AI 5도구. **AI가 자경단의 6번째 멤버**. 본인이 평생 셸을 알아야 AI도 더 잘 활용. **AI가 셸을 대체하지 않고, 셸을 가속**.

### 12-5. AI 시대 셸 학습 황금 규칙

1. 셸 80% 이해 + AI 20% 보조 = 5년 후 시니어
2. 셸 0% + AI 100% 의존 = 5년 후 답답함
3. 셸 100% + AI 0% = 시간 낭비

자경단 표준 — **80/20 비율**. 본 챕터의 8H가 80% 학습. AI는 20% 가속.

---

## 13. 추신

추신 1. 검은 화면이 본인의 평생 도구. AI 시대일수록 더 많이 만남. 1주일 안에 친해지면 평생 친구.

추신 2. 터미널·셸·Bash 셋의 차이를 한 페이지에 적기. 면접 단골 — "셸이 뭐예요?" 1분 답이 시니어.

추신 3. 4핵심 단어(터미널·셸·프로세스·파일시스템)가 본 챕터 8H의 토대. 4단어를 손가락에 박으면 H2~H8이 매끈.

추신 4. 0.30초 흐름의 7단계가 본인의 매일 1만 번. 한 번 이해하면 평생 손가락 직관.

추신 5. 자경단 alias 5종(`s`·`lg`·`ll`·`cd..`·`mypr`)을 첫 날 5분에 박기. 5년 30시간 절약.

추신 6. zsh의 oh-my-zsh + starship 조합이 자경단 표준. 셋업 5분, 평생 사용.

추신 7. 본 챕터의 H5 데모 한 줄(`find ~ ... | head -5`)을 본 H 끝에서 한 번 따라 쳐 보세요. 0.30초의 자신감.

추신 8. 본 챕터의 8H가 본인의 5년 셸 자산. 1년에 한 번씩 다시 읽으면 1년의 경험이 30%를 새로 보여 줘요.

추신 9. AI 시대의 손가락 — Claude가 본인에게 한 줄 명령어를 추천. 본인이 받아서 한 번에 실행. **AI + 터미널 = 시니어**.

추신 10. 자경단 5명이 각자의 alias를 자기 dotfiles에 박고, dotfiles를 GitHub에 공유. 5명의 dotfiles가 자경단 wiki.

추신 11. 본인의 자경단 1년 후 dotfiles는 200~500줄. 자경단 5명의 합집합은 1,000줄. 한 dotfile이 5년의 자산.

추신 12. 다음 H2는 핵심개념 — 셸 변수·환경변수·PATH·exit code·subshell·glob·redirection·heredoc 8개념 깊이. 본 H의 4단어가 H2의 8개념으로 풀려요. 🐾

추신 13. 본 H를 끝낸 본인이 한 가지 행동 — 본인 노트북에서 `tty`, `echo $0`, `ps`, `pwd` 4 명령어를 차례로 쳐 보세요. 4핵심 단어가 본인 손가락에 박힘. 4초의 실험이 평생 직관.

추신 14. 본 챕터를 다 끝낸 본인은 검은 화면의 80%를 알아요. 나머지 20%는 자경단 1년의 손가락 경험.

추신 15. 본 H의 마지막 한 줄 — **검은 화면은 본인의 평생 도구이고, 4핵심 단어가 토대이며, 0.30초 7단계가 본인의 매일 1만 번이에요. 본인의 첫 alias를 오늘 박으세요.**

추신 16. 셸의 진화 50년(10절)을 종이에 적기. 면접에서 "Bash와 zsh 차이?" 답이 1분이면 시니어 신호.

추신 17. 자경단 dotfiles 50줄(11절)을 본인 첫 1주일에 5줄로 시작. 1년 20줄, 5년 200줄. 줄이 매일을 사요.

추신 18. AI 시대 80/20 비율(12-5절)이 자경단 표준. 셸 80% + AI 20%. 0/100 의존도 100/0 비효율도 위험.

추신 19. Thompson sh(1971)에서 zsh(1990)까지 19년. 그 19년이 본인의 매일 손가락 토대. **선조에 감사**.

추신 20. macOS Catalina(2019) 라이선스 이유로 bash → zsh. GPL v3에 Apple이 부담. 그래서 macOS는 bash 3.x(GPL v2) 옛 버전 + zsh 5.x 새 버전. **라이선스가 도구 결정**.

추신 21. 자경단의 5명 dotfiles를 GitHub repo (`cat-vigilante/dotfiles`)에 공유. PR로 alias 추가 + 5명 리뷰. **dotfiles 협업이 자경단 보너스**.

추신 22. Warp 터미널은 2022년 출시. AI 통합 + 블록 단위 출력. 자경단 1년 후 도입 검토. 모든 5명이 한 도구를 쓰면 합의 비용 0.

추신 23. zsh의 `setopt nomatch`가 글로브 함정 방지. 매치 없으면 에러 → 의도 명확. bash 기본 동작과 다름.

추신 24. `~/.zshrc`의 PATH는 위에서 아래로 우선. 처음 추가가 가장 우선. brew는 Apple Silicon에서 `/opt/homebrew`, Intel에서 `/usr/local`. 위치 다름.

추신 25. iTerm2의 단축키 5종 — Cmd+T(탭)·Cmd+D(수직)·Cmd+Shift+D(수평)·Cmd+W(닫기)·Cmd+`(다음 창). 5단축이 손가락의 친구.

추신 26. tmux는 터미널 멀티플렉서. iTerm2 분할은 GUI, tmux는 셸 안 분할. SSH로 서버 접속해도 멀티 창. H3에서 깊이.

추신 27. 자경단의 dotfiles 진화 5단계 — 1주(5줄)·1개월(20줄)·6개월(50줄)·1년(100줄)·5년(200~500줄). 줄이 경험.

추신 28. AI 시대의 셸 면접 질문 — "AI가 셸을 대체할까요?" 답은 "AI가 셸을 가속한다, 대체 안 함". 셸 80% + AI 20%이 시니어.

추신 29. 본 H의 4단어(터미널·셸·프로세스·파일시스템)가 한 페이지로 압축. 면접 1분 답 — "검은 창은 터미널, 그 안에서 명령 해석은 셸, 실행 단위는 프로세스, 모든 입출력은 파일시스템".

추신 30. 0.30초의 7단계가 본인 머리에 박히면 사고 시 5초 진단. "왜 명령어 안 돼?" → fork 단계? exec 단계? PATH 문제? 7단계로 분리 진단.

추신 31. PATH 검색은 `:` 구분 디렉토리 순서대로. 같은 이름 명령이 두 곳 있으면 첫 번째 사용. 그래서 `~/.local/bin`을 PATH 앞에 두면 우선.

추신 32. `which command`로 어느 PATH에서 명령어가 오는지 확인. `which python`이 둘 이상이면 환경 충돌 신호.

추신 33. `type command`는 alias·function·built-in·외부 명령 중 어느 종류인지 알려줌. `type ls` → "ls is /bin/ls". `type cd` → "cd is a shell builtin".

추신 34. 셸 built-in vs 외부 명령 — built-in은 셸 자체에 박힌 (cd·echo·export·source). 외부는 별도 프로그램 (ls·grep·find). built-in이 빠름.

추신 35. `cd`는 built-in이어야 한다 — 외부 명령이면 fork 후 자식의 cwd 바뀌고 부모(셸)는 안 바뀜. **fork 분리가 cd를 built-in으로**.

추신 36. 자경단 5명의 .zshrc 평균 길이 5년 후 200줄. 본인이 매년 30~50줄 추가. 줄이 매일의 절약.

추신 37. dotfiles GitHub 공개 시 비밀 분리 — `~/.zshrc.local`로 token·secret 분리. 공개는 alias·function만.

추신 38. zsh의 `oh-my-zsh`는 plugin 200개 + 테마 100개. 자경단 표준 plugin — git·docker·kubectl·npm·rust·gh 6개.

추신 39. starship 프롬프트는 Rust 작성. 빠름. git status·언어·실행 시간을 프롬프트에 표시. 자경단 5명 모두 사용 권장.

추신 40. 자경단의 첫 dotfile 셋업 — Ch006 H3에서 5분. iTerm2·oh-my-zsh·starship·brew 4종. 한 번 하면 평생.

추신 41. 본 챕터의 8H가 자경단 4주 학습 (주당 H1·H2 같이). 8주 슬랙도 가능. 본인 페이스에 맞게.

추신 42. 셸 함수 vs alias — alias는 단순 치환, 함수는 인자·로직. 자경단 권장 — 단순한 건 alias, 복잡한 건 함수.

추신 43. 함수의 첫 줄 `set -euo pipefail`이 안전 표준. e=에러 시 exit, u=정의 안된 변수 에러, o pipefail=파이프 한 부분 실패도 탐지. H6에서 깊이.

추신 44. 본 챕터의 자경단 alias 5종을 본인이 첫 H 끝에 한 번 박고 1주일 사용 → 효과 체감 후 추가. **점진적 도입이 평생 유지**.

추신 45. 셸 학습의 황금 규칙 — 매일 새 명령어 1개. 30일 = 30개. 1년 = 365개. 본 챕터 30개 + 1년 365개 = 평생 자산.

추신 46. 본 챕터를 다 끝낸 본인은 셸의 80%. 나머지 20%는 5년 누적 손가락 + 사고 학습.

추신 47. 자경단 5명이 다른 stack(백엔드 Python·프론트 React·인프라 AWS)인데도 셸 80%는 같음. **셸이 5명의 공통 언어**.

추신 48. 본 H의 한 줄 데모 (`find ~ -type f -size +100M ...`)가 H4·H5에서 자세히. 본 H에선 미리 보기.

추신 49. 본 H의 8H 큰그림(6절)을 종이에 적어 모니터 옆에. 본인이 어디 단계인지 매주 확인.

추신 50. **다음 H2** 핵심개념 — 셸 변수·환경변수·PATH·exit code·subshell·glob·redirection·heredoc 8개념. 본 H의 4단어가 H2의 8개념으로 확장. 🐾

추신 51. 셸 학습은 평생. 5년 후엔 본인이 새 신입에게 본 챕터 가르침. 그날 본인이 본 챕터의 첫 학습자였음을 기억.

추신 52. AI 시대 셸의 가장 큰 변화 — 본인이 명령어 모를 때 5분 → 1초. 그 차이가 5년 누적 시간 산. 본인의 시간 가치.

추신 53. zsh의 tab 자동완성은 옵션 + 인자 + 파일까지 제안. bash보다 강력. 자경단 표준이 zsh인 이유 중 하나.

추신 54. `git commit -m "..."` 칠 때 zsh는 `-m` 옵션 + 메시지 자동 인지. 손가락 절약.

추신 55. 자경단의 5년 후 dotfiles는 200줄 본인 + 100줄 까미 + ... = 합 700줄. 그 합이 5명의 평생 손가락 자산.

추신 56. 본 챕터의 모든 명령어를 본인 노트북에서 한 번씩 따라치면 평생 직관. 1주일 안에 손가락이 자동.

추신 57. 본 H를 두 번 읽으세요. 첫 번째는 흐름, 두 번째는 한 단어 깊이. 두 번 읽기가 한 번 따라치기와 같은 효과.

추신 58. 자경단의 첫 dotfile commit 메시지 — `feat: 본인 .zshrc 첫 5줄`. 그 commit이 본인의 5년 dotfile 진화의 첫 줄.

추신 59. 본 챕터를 끝낸 본인이 한 가지 결심 — 본인 dotfiles를 GitHub repo로. 1주일 안에 첫 push. 5년 후 본인의 자산.

추신 60. 본 H의 마지막 진짜 한 줄 — **검은 화면은 본인의 평생 친구이고, AI 시대일수록 더 가까이 만나며, 80%의 셸 학습 + 20%의 AI가 시니어의 비율이에요. 본인의 첫 alias를 오늘 박으세요.**

추신 61. 자경단 5명 alias 풍경(5-1절)에서 까미·노랭이·미니·깜장이의 alias가 다 다른 이유 — 각자 stack이 다름. 같은 명령어를 100번 치는 사람의 alias가 그 사람의 일상.

추신 62. 본 H의 8H 큰그림(6절) 중 H5(데모)가 자경단의 심장. "내 맥에서 가장 큰 파일 5개" 한 줄이 본인의 첫 셸 자신감.

추신 63. 12회수 지도(7절)의 12 챕터가 본 챕터의 입장권. Ch007 Python부터 본인의 셸이 매일 살아 움직임.

추신 64. 흔한 오해 5(8절) 중 가장 큰 오해 — "맥북 사용자는 터미널 안 배워도 돼요". 거꾸로. 맥북이 Unix 기반이라 터미널이 자연. Windows 사용자가 터미널 배우기 더 어려움.

추신 65. FAQ Q1(zsh vs bash)의 답 — 자경단 표준 zsh + Linux 서버 bash. 90% 호환이라 둘 다 거의 같음. 차이는 인터랙티브(자동완성·테마)에서.

추신 66. AI 시대의 셸 12절은 본 챕터의 가장 새로운 부분. 1년 전엔 없었어요. 본 챕터 1년 후엔 더 깊어짐. **AI 시대가 셸 진화의 가속**.

추신 67. 자경단 dotfiles 50줄(11절)은 본인이 한 번에 만들지 마세요. 1주일에 5줄씩. 매주 수요일 17:00 dotfile 갱신 시간 5분.

추신 68. zsh의 oh-my-zsh + starship 조합이 자경단의 기본. 1년 후 자기만의 plugin 추가. 200개 plugin 중 본인이 좋아하는 5개.

추신 69. 본 챕터를 다 끝낸 본인이 자경단 wiki에 "셸 첫 1주일 가이드" 한 페이지 작성. 5명 모두 같은 페이지를 봄. 자경단 합의 비용 0.

추신 70. 셸의 50년 진화(10절)에서 가장 큰 변화점은 1989년 Bash 출시. GNU 표준이 되면서 모든 Linux의 기본. 자경단의 95% 도구가 Bash 호환.

추신 71. zsh가 macOS 기본 된 2019년 이후 Linux 사용자도 zsh 검토. "Apple이 골랐으면 좋은 거"의 신호. 다만 서버는 여전히 bash.

추신 72. fish 셸은 사용자 친화로 유명. 자경단 표준은 아니지만 1년 후 한 명이 시도하기도. 본인 취향이 본인의 도구.

추신 73. 본 H의 마지막 한 가지 — 본 H를 끝낸 그 5분에 본인 노트북에서 4 명령어(`tty`·`echo $0`·`ps`·`pwd`)를 한 번씩 쳐 보세요. 4단어가 손가락에 박힘.

추신 74. 자경단 5명의 dotfiles 합집합 1,000줄이 자경단의 진짜 협업 자산. 한 사람의 5년 손가락이 다른 사람에게 5분 학습으로.

추신 75. AI 시대의 셸 80/20 비율은 본인의 학습 가이드. 본 챕터 8H가 80% 학습. AI(Claude·ChatGPT)가 20% 가속. 비율을 잊지 마세요.

추신 76. 본 챕터의 모든 명령어는 macOS·Linux 호환. Windows는 WSL2(Windows Subsystem for Linux)로 같은 명령어 사용. **Unix가 표준**.

추신 77. 자경단 첫 dotfiles repo 이름 — `cat-vigilante/dotfiles`. 5명이 PR로 추가·갱신. README에 "본 dotfiles 5분 셋업 가이드" 한 페이지.

추신 78. 본 H의 진짜 마지막 회고 — 검은 화면은 본인의 평생 도구이고, 본 챕터 8H가 5년의 토대이며, AI 시대가 셸을 가속해요. 본 챕터를 시작한 그 마음이 본인의 5년 손가락의 시작.

추신 79. 본인이 본 H를 끝낸 그 시점이 본인의 셸 첫 시작. 그날을 기억하세요. 5년 후 회고에서 "그날부터 셸이 친구가 됐어요"가 본인의 한 줄.

추신 80. **본 챕터의 진짜 첫 손가락** — 본인 노트북에서 `tty` 한 번 치고 출력 보고 한 번 호흡. 그 한 호흡이 본인의 5년 셸 자신감의 시작.

추신 81. 자경단 5명이 다같이 같은 명령어를 칠 때 5명 화면이 동시에 같은 출력. 그 동기화의 짜릿함이 협업의 비밀. 셸이 5명 같은 결과 보장.

추신 82. 본인의 첫 셸 명령어 사고 — 평생 첫 사고. 보통 `rm`을 잘못 써서 파일 날림. 본 챕터의 H4 위험도 신호등에서 처방.

추신 83. AI 시대의 셸은 본인이 명령어를 짧게 알아도 OK. AI가 30% 보충. 다만 80%는 본인. 본 챕터의 80%가 그 80%.

추신 84. 본 H의 4단어(터미널·셸·프로세스·파일시스템)는 Unix 50년의 압축. 4단어를 안 본인은 50년의 후예.

추신 85. 0.30초 흐름 7단계는 매일 1만 번 발동. 1년 365만 번. 5년 1,800만 번. 한 흐름이 평생.

추신 86. 본 챕터의 자경단 합의 — 5명 모두 zsh + iTerm2 + oh-my-zsh + starship. 동일 환경이 협업의 기본.

추신 87. dotfiles GitHub 공유 시 5명의 OS·하드웨어 차이를 흡수하는 한 줄 — `if [[ "$OSTYPE" == "darwin"* ]]; then ... fi`. macOS·Linux 분기.

추신 88. 본 H를 다 읽은 본인이 한 가지 결심 — 본인의 첫 dotfiles repo를 1주일 안에 만들고 첫 PR로 5줄 alias. 5년의 시작.

추신 89. 자경단 1년 후엔 본인의 dotfiles가 50줄. 5년 후엔 200줄. 1년 50줄 ÷ 365일 = 매일 0.14줄. **매일 한 줄도 안 되는 노력이 5년 자산**.

추신 90. 본 챕터의 진짜 결론 — 셸은 본인의 평생 도구이고, AI 시대일수록 더 가까이 만나며, 80% 학습이 평생, 20% AI가 가속. 본인의 첫 alias를 오늘.

추신 91. 셸의 zsh 진화 50년이 본인의 매일 손가락 토대. 1971년 Thompson이 첫 sh를 작성한 그날, 본인이 2026년에 zsh 치고 있을 줄 몰랐을 거. **선조의 한 줄이 후예의 평생**.

추신 92. 본 H의 8H 큰그림(6절)에서 H6(스크립트)이 자경단 운영의 자동화 시작. 매일 30분 셋업 의식을 한 스크립트로. 30분 셋업 → 30초 셋업.

추신 93. 본 챕터를 끝낸 본인의 첫 한 가지 — 본인 자경단 5명 합의로 dotfiles repo 만들기. 5명이 PR로 alias 추가. 1주일에 한 alias.

추신 94. AI 시대의 셸 학습 황금 비율 — 셸 80% + AI 20%. 80%가 본 챕터 8H, 20%가 매일 Claude·ChatGPT 추천.

추신 95. 본 H의 마지막 회고 — 검은 화면이 본인의 평생 도구이고, 4핵심 단어가 토대이며, 0.30초 7단계가 본인의 매일 1만 번이고, AI 시대가 셸을 가속해요.

추신 96. 본 챕터를 다 끝낸 본인이 새 신입에게 셸을 가르칠 때 본 H를 권장. 4단어 + 7단계 + 8H 큰그림 + alias 5종 + AI 시대가 본인의 평생 가르침.

추신 97. 자경단의 1년 후 회고에서 가장 자주 나오는 문장 — "Ch006 안 봤으면 매일 30분 손해". 본 챕터가 매일 손가락의 면역.

추신 98. 본인이 본 H를 다 읽은 그 시점이 본인의 셸 손가락 첫 시작. 그날의 짜릿함을 기억하세요. 5년 후 회고의 첫 줄.

추신 99. 본 챕터의 진짜 마지막 — 검은 화면은 본인의 평생 친구이고, 본 챕터가 그 친구의 첫 만남이며, AI 시대일수록 더 가까이 만나요. 본인의 첫 5분이 5년의 시작.

추신 100. **본 H 진짜 마지막** — 본인의 첫 alias 5종을 오늘 박으세요. 그 5분이 5년의 시작이고, 5년이 본인의 평생이에요.

추신 101. 본 챕터를 다 끝낸 본인이 자경단 5명에게 본 H를 권하면 5명 모두 같은 80%의 셸 직관. 5명이 같은 페이지를 보면 합의 비용 0.

추신 102. 본 H의 4단어(터미널·셸·프로세스·파일시스템)는 OS 50년의 압축. Unix 1971년부터 본 챕터 작성 2026년까지의 모든 셸이 4단어로 압축.

추신 103. AI 시대의 셸 학습이 옛날보다 5배 빠름. 본 챕터 8H가 옛날 40H 학습 효과. AI 가속의 산물.

추신 104. 본인의 자경단 1주일 후엔 본 H의 4단어와 0.30초 7단계가 머릿속에 자동. 면접에서 한 줄 설명이 1분 안에. **시니어의 첫 신호**.

추신 105. 본 H의 진짜 결론 한 줄 — **검은 화면이 본인의 평생 친구이고, 본 챕터 8H가 그 친구의 첫 만남이며, 본인의 첫 alias 5종을 오늘 박으세요.**

추신 106. 본 챕터를 끝낸 본인이 본 H의 4단어 + 0.30초 7단계 + 8H 큰그림 + alias 5종 + AI 80/20 비율을 모두 안 사람. 시니어의 첫 단계.

추신 107. 자경단 5명이 본 챕터를 끝낸 후 5명의 평균 dotfiles 50줄. 5명 합 250줄. 본인의 250줄이 자경단 1년의 손가락 자산.

추신 108. 본 챕터의 한 가지 약속 — 본인이 본 H를 끝내고 1주일 안에 첫 dotfile 5줄을 박지 않으면 본 챕터의 효과는 0. **행동이 학습**.

추신 109. 본 H의 마지막 명령 — 지금 본인 노트북 터미널 열고 `tty` 한 번 치세요. 출력 보고 한 호흡. 그 한 호흡이 본 챕터의 첫 손가락.

추신 110. **본 H 끝** ✅ — 본인의 셸 첫 1시간 학습 완료. 다음 H2에서 셸의 8개념 깊이.

추신 111. 셸의 50년 진화 + 자경단 5년 dotfiles + AI 시대 80/20 비율의 셋이 본 H의 최대 깊이. 셋을 한 페이지에 적어 두세요.

추신 112. 본 H의 어느 추신이 본인에게 가장 와닿았나요? 그 추신 한 줄을 본인 dotfile의 첫 주석으로 박기. **추신이 본인의 매일 영감**.

추신 113. 자경단의 진짜 검은 화면 친구가 되는 한 가지 표현 — 본인이 1주일 안에 100번 명령어를 칠 수 있게 본 H의 4단어를 손가락에 박는 것. 100번이 직관의 시작.

추신 114. 본 챕터의 마지막 의식 — 본인이 본 H를 다 읽었음을 자축. 셸의 첫 80%가 본인 손에 있어요. 다음 H2부터 깊이로.

추신 115. **본 H의 진짜 진짜 마지막** — 본인 노트북 터미널을 지금 열고 `tty` + `echo $0` + `ps` + `pwd` 4번 차례로 치세요. 4초의 손가락이 본인의 5년 자신감.

추신 116. 본 H를 다 끝낸 본인이 가장 큰 변화 — 검은 화면이 무섭지 않아요. 그 변화 하나가 5년의 차이.

추신 117. 본 챕터의 8H가 본인의 셸 첫 1주일 또는 1개월. 본 H가 그 첫 1시간. 1시간 × 8 = 8시간이 본인의 5년 셸 토대.

추신 118. 본 H의 모든 추신을 다 안 외워도 OK. 마지막 5개(추신 116~120)만 마음에. 평생 사전.

추신 119. 자경단 5명이 본 챕터를 같이 끝내면 5명 모두 같은 80%의 셸 직관. 같은 페이지를 보면 합의 비용 0.

추신 120. **본 H의 진짜 진짜 진짜 마지막** — 검은 화면은 본인의 평생 친구이고, 본 챕터 8H가 그 첫 만남이며, 본인의 첫 alias를 오늘 박으세요. 🐾🐾🐾🐾🐾🐾🐾🐾 본 H의 끝이고 다음 H2의 시작이에요.
